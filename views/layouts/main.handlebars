<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-100">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Get Higher!</title>
  <link rel="stylesheet" href="/css/output.css">
</head>

<body class="h-full">
  
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center p-2">
      <h1 class="">
        <span role="img" aria-label="money">💸</span>
        <a href="/">Get Higher!</a>
      </h1>
      <!-- This example requires Tailwind CSS v2.0+ -->
    <div class="bg-gray-50">
      <div class="max-w-7xl mx-auto py-12 px-4 sm:px-6 lg:py-16 lg:px-8 lg:flex lg:items-center lg:justify-between">
        <h2 class="text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl">
          <span class="block text-indigo-600">Get paired with a job today!</span>
        </h2>
        <div class="mt-8 flex lg:mt-0 lg:flex-shrink-0">
        </div>
      </div>
    </div>

      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile" class="inline-flex items-center justify-center px-5 py-3 border border-transparent text-base font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700">profile</a> |
        <button class="no-button inline-flex items-center justify-center px-5 py-3 border border-transparent text-base font-medium rounded-md text-indigo-600 bg-white hover:bg-indigo-50" id="logout">logout</button>
        {{else}}
        <a href="/login" class="inline-flex items-center justify-center px-5 py-3 border border-transparent text-base font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700">Log In</a>
        {{/if}}
        <ul>
        <li>
          <a href="/">Home</a>
        </li>
        <li>
          <a href="/profile">Profile</a>
        </li>
        <li>
          <a href="/jobs">All Jobs</a>
        </li>
        <li>
          <a href="/quicksearch">Quick Search</a>
        </li>
      </ul>
      </nav>
    </header>
    <main>
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
        <!-- This example requires Tailwind CSS v2.0+ -->
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="../../public/js/logout.js"></script>
  {{/if}}
  <script src="../../public/js/jobs.js"></script>
</body>

</html>
