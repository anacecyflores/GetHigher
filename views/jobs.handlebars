{{!-- <div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Job Postings JUST for you:</h3>

    <form class="form new-jobs-form">
      <div class="form-group">
        <label for="jobs-name">jobs name:</label>
        <input class="form-input" type="text" id="jobs-name" name="jobs-name" />
      </div>
      <div class="form-group">
        <label for="jobs-funding">needed funding ($):</label>
        <input class="form-input" type="number" id="jobs-funding" name="jobs-funding" />
      </div>
      <div class="form-group">
        <label for="jobs-desc">description:</label>
        <textarea class="form-input" id="jobs-desc" name="jobs-desc"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Create</button>
      </div>
    </form>
  </div>

  {{#if jobs.length}}
  <div class="col-md-6 jobs-list">
    <h3>Current Jobs:</h3>

    {{#each jobs}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/jobs/"{{jobs.id}}>{{jobs.title}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{jobs.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div>

<script src="./js/jobs.js"></script> --}}
<!-- This example requires Tailwind CSS v2.0+ -->
<div class="bg-indigo-600">
  <div class="max-w-7xl mx-auto py-3 px-3 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between flex-wrap">
      <div class="w-0 flex-1 flex items-center">
        <span class="flex p-2 rounded-lg bg-indigo-800">
          <!-- Heroicon name: outline/speakerphone -->
          <svg class="h-6 w-6 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M11 5.882V19.24a1.76 1.76 0 01-3.417.592l-2.147-6.15M18 13a3 3 0 100-6M5.436 13.683A4.001 4.001 0 017 6h1.832c4.1 0 7.625-1.234 9.168-3v14c-1.543-1.766-5.067-3-9.168-3H7a3.988 3.988 0 01-1.564-.317z" />
          </svg>
        </span>
        <p class="ml-3 font-medium text-white truncate">
          <span class=""> Big news! We're excited to announce our brand new Quick Search Feature. </span>
        </p>
      </div>
      <div class="order-3 mt-2 flex-shrink-0 w-2/4 sm:order-2 sm:mt-0 sm:w-auto">
        <a href="/quicksearch" class="flex items-center justify-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-indigo-600 bg-white hover:bg-indigo-50"> Try Now! </a>
      </div>
    </div>
  </div>
</div>

<section id="jobs" class="container">
  <header class="bg-white shadow">
    <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
      <h1 class="text-3xl font-bold text-gray-900">Saved Jobs</h1>
    </div>
  </header>
  <main>
    <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
      <!-- Replace with your content -->
      <div class="px-4 py-6 sm:px-0">
        <div class="border-4 border-dashed border-gray-200 rounded-lg h-96">
                  {{#each jobs}}
            <div class="job">
              <h3>{{title}}</h3>
              <p>{{employer}}</p>
              <ul>
                <li>City: {{location_city}}</li>
                <li>State: {{location_state}}</li>
                <li><a href="{{apply_link}}" class="btn btn-reverse">Apply Now</a></li>
                <li><a href="#" class="btn btn-reverse">Remove Job</a></li>
              </ul>
              <div class="tech">
                <small>Listed by: <span>{{publishing_site}}</span></small>
              </div>
            </div>
          {{else}}
          <p>No jobs currently saved</p>
          {{/each}}
        </div>
      </div>
      <!-- /End replace -->
    </div>
    
  </main>

  </section>

